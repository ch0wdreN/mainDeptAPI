allow:
  - run
  - write
  - read
  - net
  - env

envFile:
  - .env

scripts:
  start:
    desc: Start local server
    cmd: deno run --watch --unstable --compat ./main.ts

  fmt:
    desc: deno fmt
    cmd: deno fmt

  test:
    desc: deno test
    cmd: deno test --watch .